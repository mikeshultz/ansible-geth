[Unit]
Description=go-thereum client
Requires=network.target

[Service]
Type=simple
User={{ geth_user }}
ExecStart=/home/{{ geth_user }}/.local/bin/geth --datadir "{{ mainnet_datadir }}" --cache {{ geth_cache }} --syncmode "fast"
Restart=on-failure

[Install]
WantedBy=default.target